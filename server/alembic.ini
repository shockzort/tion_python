# CLI-обёртка для разработчика: uv run alembic revision --autogenerate -m "..."
# Сервис применяет миграции сам (Database.migrate), ini ему не нужен.

[alembic]
script_location = src/easy_breezy/storage/migrations
sqlalchemy.url = sqlite+aiosqlite:///data/easy_breezy.db
